    
name: Prod | Deploy Bicep Registry

on:
  workflow_dispatch:
    inputs:
      what_if_only:
        description: Run a what-if analysis without making changes
        type: boolean
        default: true

  push:
    branches:
      - master
    paths:
      - './infra/*.bicep'
      - './infra/params/*.prod.bicepparam'

  pull_request:
    branches:
      - master
    paths:
      - './infra/*.bicep'
      - './infra/params/*.prod.bicepparam'
   
jobs:
  deploy_prod:
    uses: ./.github/workflows/tmpl.bicep.infra.yaml
    #
    # Set a secret named AZURE_CICDSPN either:
    # - in the environment
    # - or repository scoped
    #
    # The secret should be jsonencoded like this:
    # {
    #   "clientId": "00000000-0000-0000-0000-000000000000",
    #   "objectId": "00000000-0000-0000-0000-000000000000",
    #   "clientSecret": "00000000-0000-0000-0000-000000000000",
    #   "subscriptionId": "00000000-0000-0000-0000-000000000000",
    #   "tenantId": "00000000-0000-0000-0000-000000000000"
    # }
    with:
      # NOTE:
      # You can add an approval gate via the environment deployment protection rule for approvers
      # environment: prod

      scope: resource_group
      resource_group: 'sample-prod'
      
      deployment_name: 'sample-prod'
      template_file: ./infrastructure/registry.bicep
      parameter_file: ./infrastructure/params/registry.prod.bicepparam
      
      what_if_only: ${{ github.EVENT_NAME  == 'pull_request' || inputs.what_if_only == true }}

      # set_temporary_ip_rules: |
      #   acr-sample-prod
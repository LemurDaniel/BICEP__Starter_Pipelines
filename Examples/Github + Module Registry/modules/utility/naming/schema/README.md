# **Bicep Naming-Module**

These naming schemas can be maintained in a seperate repo, file, container registry, etc.

The only important part is, that bicep can provide a schemaReference as an bicep-object to the used module.